# 콘텐츠 자동 생성기

오블리브 송도 호라이즌의원 문제성발톱클리닉의 블로그 콘텐츠를
**의료법을 지키면서** 구글·네이버 SEO, AEO, GEO에 맞게 생성합니다.

```bash
pip install -r requirements.txt
cp .env.example .env      # ANTHROPIC_API_KEY와 병원 기본정보 입력
python main.py content    # http://localhost:5001
```

## 제목만 넣고 한 번에

```bash
python main.py auto "송도 발톱무좀 병원 선택 기준"
python main.py auto titles.txt      # 한 줄에 제목 하나씩, 여러 편
python main.py auto "제목" --draft   # 업로드 없이 원고만
python main.py auto "제목" --hidden  # 미노출로 업로드
```

제목에서 카테고리·주키워드를 추론 → 생성 → 검사 → 썸네일 → CMS 업로드까지 한 번에 돕니다.
웹 UI 첫 화면에도 같은 기능이 있습니다.

**검사를 통과하지 못한 글은 업로드하지 않습니다.** 이 게이트는 설정으로 끌 수 없습니다.
자동화의 값어치는 잘못된 글을 안 올리는 데 있습니다.

| 게이트 | 통과 못 하면 |
|---|---|
| 의료법 위반 표현 | 최대 3회까지 다시 쓰고, 그래도 남으면 업로드 중단 |
| 필수 기재 누락 | 동일 |
| 본원 미시행 시술 언급 | 동일 (허위광고에 해당) |

---

## 왜 이렇게 만들었나

병원 콘텐츠는 일반 마케팅 글과 제약 조건이 다릅니다.

| 제약 | 대응 |
|---|---|
| 의료법 제56조 위반 시 업무정지·과징금 | 생성 후 26개 규칙으로 자동 검사, 위반 시 다시 씀 |
| 부작용 등 중요정보 누락 금지(제5호) | 필수 고지문을 시스템이 항상 덧붙임 (모델이 빠뜨려도 들어감) |
| AI가 없는 사실을 지어냄 | `content/clinic.py`에 적힌 사실만 쓰도록 프롬프트에서 차단 |
| CMS 글자수 하드 리밋 | MetaTitle 40자·MetaDescription 80자를 생성 단계에서 강제 |

즉 **글을 뽑는 것**이 아니라 **발행해도 되는 글인지 판정하는 것**이 이 앱의 핵심입니다.

---

## 세 가지 최적화 축

같은 글이라도 검색엔진·답변엔진·생성형 검색이 보는 지점이 다릅니다.
셋을 따로 채점해서, 어느 축이 부족한지 바로 보이게 했습니다.

### SEO — 구글·네이버 검색 결과
- MetaTitle 40자 / MetaDescription 80자 준수 + 주키워드 포함
- H1에 주키워드 **와** 지역명 동시 포함
- 포스트 URL 슬러그 자동 생성 (제목 → 구두점 제거 → 공백을 `-`로)
- 주키워드 밀도 0.5~2.5% 유지, 본문 1,500자 이상
- 한글 띄어쓰기 무시 매칭 (`발톱 무좀` = `발톱무좀`)

### AEO — 네이버 스마트블록 · 구글 AI 개요에 발췌
- **답변 캡슐**: 글 맨 앞에 제목이 던진 질문의 직답 2~3문장
- 소제목의 절반 이상을 실제 검색 질의 형태로 (`발톱무좀은 왜 약을 발라도 낫지 않나요?`)
- 모든 H2 바로 아래에 **결론 먼저** 2~3문장 (`direct-answer`)
- FAQ 4~6개 → `FAQPage` 구조화 데이터로 자동 변환
- 목차 앵커 (id 앵커 + 텍스트 프래그먼트 병행)

### GEO — ChatGPT·Claude 등 생성형 검색이 인용
- **핵심 요약**: 앞뒤 문맥 없이 잘라도 뜻이 통하는 완결 문장 3~5개
  (지시어로 시작 금지, 주어 명시 — 인용될 때 그대로 쓰이는 문장)
- 비교·분류 정보는 표로 (생성형 검색이 구조를 그대로 읽음)
- 기관명 표기 통일 — 첫 등장 정식명칭, 이후 축약형 고정
- 질병관리청·식약처 등 공신력 있는 근거 기관 언급 + 출처 목록
- `MedicalClinic` / `MedicalWebPage` / `FAQPage` / `BreadcrumbList` JSON-LD

---

## 생성 파이프라인

```
입력(카테고리·주키워드·주제)
   ↓
Claude Opus 5 (Structured Outputs로 고정 스키마 JSON 강제)
   ↓
후처리 — 슬러그 생성, 해시태그 정규화, 표 행 길이 보정, 글자수 검증
   ↓
의료법 검사 (26개 규칙 + 필수 기재 3항목)
   ↓
SEO/AEO/GEO 채점 (29개 항목)
   ↓
위반 있거나 80점 미만이면 → 지적사항을 담아 자동 재작성 (1회)
   ↓
CMS 필드 + 본문 HTML + 평문 + JSON-LD 출력
```

시스템 프롬프트는 매번 동일하므로 프롬프트 캐싱을 걸어 두었습니다.
2편째부터 입력 토큰 비용이 크게 줍니다.

---

## 화면

### 생성 (`/`)
카테고리를 고르면 주키워드 후보가 버튼으로 뜹니다.
`config.py`의 순위 모니터링 키워드와 짝을 이루도록 구성해서,
생성한 글의 성과를 기존 순위 대시보드(`python main.py dashboard`)에서 바로 추적할 수 있습니다.

### 결과 (`/post/<id>`)
- **CMS 기본 정보** — 관리자 화면 필드 순서 그대로. 항목별 복사 버튼, 글자수 카운터
- **미리보기 / HTML / 평문 / JSON-LD / 항목별 점검** 5개 탭
- 의료법 위반이 있으면 조문·해당 문구·수정 방향을 함께 표시

### 원고 검사 (`/lint`)
이미 써 둔 원고를 붙여 넣어 의료법 표현만 점검합니다.
**기존 발행 글 점검용으로도 씁니다.**

---

## 의료법 검사 규칙

의료법 제56조 제2항 각 호 기준. `content/medical_law.py`에 정의되어 있습니다.

| 조항 | 잡아내는 표현 |
|---|---|
| 제2호 치료효과 보장 | 완치 보장, 100%, 재발 없음, 반드시 낫습니다, 무통, 부작용 없음 |
| 제3호 비교·비방 | 타 병원보다, 다른 병원과 달리 |
| 제4·5호 시술 노출·중요정보 누락 | 전후 사진, 시술 장면 |
| 제6호 과장 | 최고, 1위, 유일, 국내 최초, 권위자, 명의, 출처 없는 성공률 |
| 제7호 법적 근거 없는 명칭 | 전문병원, 특화병원, 공식 지정병원 |
| 제8호 기사형 광고 | 기자, 취재, 보도에 따르면, 방송 출연 |
| 제12호 치료경험담 | 환자 후기, 실제 후기, 체험담 인용 |
| 제13호 인증·추천 | 수상, 감사장, 공식 인증, 추천 병원 |
| 제14호 환자 유인 | 할인, 무료, 이벤트가, 선착순, 반값, 구체적 진료비 |
| 기타 | 임신부·소아 안전성 단정, 자가 처치 권유, 즉시 예약 압박 |

**필수 기재 3항목** (없으면 발행 불가로 판정):
부작용 안내 · 치료 결과의 개인차 · 진료 상담 안내

> 자동 검사는 초안에서 위험 표현을 걸러내기 위한 것입니다.
> 법률 자문이나 의료광고 사전심의를 대체하지 않습니다. 최종 판단은 사람이 합니다.

---

## 진료 분야와 포지셔닝

`content/services.py` 하나가 "무엇을 진료하는 병원인가"를 정합니다.
카테고리·키워드·해시태그·허용 사실·금지 표현이 모두 여기서 나옵니다.

### 현재 설정 (문제성발톱)

| 항목 | 값 |
|---|---|
| 강점 | 문제성발톱치료 · 내성발톱 비수술 교정치료 · 발톱무좀 레이저치료 |
| 시행하지 않음 | 내성발톱 절개, 발톱 수술·제거술 (언급 시 **업로드 차단**) |
| 지양 표현 | 스케일링, 관리/케어 프로그램, "약만 처방" |

`content/positioning.py`가 이를 검사합니다. 의료법 검사와 분리한 이유는,
의료법은 어느 병원에나 같지만 포지셔닝은 병원마다 다르기 때문입니다.

**문맥을 봅니다.** "본원은 발톱을 뽑지 않고 교정합니다"나
"절개가 필요하면 외과 진료가 필요할 수 있습니다"는 잡지 않습니다.
본원이 그 시술을 하는 것처럼 쓴 경우만 차단합니다.

### 새 진료 분야 추가 (피부 시술 등)

`content/services.py`의 `_TEMPLATE`을 복사해 `SERVICES`에 넣고 전환합니다.

```bash
CONTENT_SERVICE=피부 python main.py auto "리쥬란 vs ECM 차이"
```

항목이 비어 있으면 생성을 막습니다. 빈 사실로 글을 쓰지 않기 위해서입니다.
`categories`는 실제 CMS 드롭다운과 일치해야 하는데,
`inspect-cms`를 돌렸다면 CMS에서 읽은 실제 목록으로 자동 대체됩니다.

---

## 설정

병원 기본정보는 `.env`로 관리합니다. **홈페이지·네이버플레이스·구글 비즈니스 프로필과
글자 단위로 동일해야** 지역 검색과 AI 인용에서 같은 병원으로 인식됩니다.

```bash
CLINIC_STREET=송도과학로 ...       # 비면 JSON-LD에서 주소가 빠짐
CLINIC_PHONE=032-000-0000
CLINIC_HOURS=Mo-Fr 10:00-19:00, Sa 10:00-14:00
CLINIC_NAVER_PLACE=https://...
CLINIC_AUTHOR_NAME=홍길동          # 구글 건강 콘텐츠(E-E-A-T) 평가에 직접 영향
CLINIC_AD_REVIEW_NO=               # 심의번호 있으면 본문 하단 자동 표기
```

미입력 항목은 화면 상단에 경고로 뜨고 GEO 점수가 깎입니다.

병원이 실제로 보유한 장비·치료 절차·진료 범위는 `content/clinic.py`에 있습니다.
**여기에 없는 내용은 생성되지 않습니다.** 장비를 추가하면 이 파일을 고치세요.

---

## CLI

```bash
python main.py write 발톱무좀치료 발톱무좀병원 "진료과 선택 기준"
python main.py write        # 대화형
python test_content.py      # API 호출 없이 전체 파이프라인 점검
```

## API

```bash
curl -X POST localhost:5001/api/lint \
     -H 'Content-Type: application/json' \
     -d '{"text":"검사할 원고"}'

curl localhost:5001/api/post/1     # CMS 필드 + 본문 HTML + JSON-LD
```

---

## CMS 자동 업로드

생성한 글을 `blog.obliv.kr` 관리자에 자동으로 올립니다.
브라우저를 실제로 띄워 로그인 → 폼 입력 → 썸네일 업로드 → 저장까지 수행합니다.
(레거시 PHP 관리자는 세션·CSRF·파일 업로드가 얽혀 있어 요청 위조보다 브라우저 조작이 안정적입니다.)

### 안전장치

자동화가 병원 블로그에 잘못된 글을 **공개**시키는 것이 가장 비싼 사고입니다.
그래서 기본값을 다음과 같이 잡았습니다.

| 장치 | 기본 동작 |
|---|---|
| 의료법 검사 | 위반 표현이 남아 있으면 **업로드 자체를 거부** |
| 노출 여부 | 항상 **미노출**로 저장. 공개 전환은 사람이 CMS에서 직접 |
| 연습 실행 | 폼만 채우고 저장은 누르지 않음 (웹 UI 기본 체크됨) |
| 화면 캡처 | 저장 직전·직후를 `cms_shots/`에 기록 |
| 확인 대화상자 | 실제 저장 시 한 번 더 확인. 노출 선택 시 경고 문구 추가 |

### 설정

```bash
# .env  ── 로그인 정보는 절대 커밋하지 마세요
CMS_WRITE_URL=https://blog.obliv.kr/blogmanage/nxt-blog.php
CMS_LOGIN_URL=          # 비우면 글쓰기 URL 접속 후 리다이렉트를 따라감
CMS_USERNAME=
CMS_PASSWORD=
CMS_HEADLESS=1          # 0으로 두면 브라우저 창을 띄워 눈으로 확인
```

### 1단계 — 폼 구조 읽기 (최초 1회)

셀렉터를 코드에 박아 두지 않습니다. 실제 화면을 훑어 매핑 파일을 만듭니다.

```bash
python main.py inspect-cms          # 헤드리스
python main.py inspect-cms --show   # 브라우저 창을 띄워 확인
```

관리자 화면의 라벨(`제목( H1 )`, `MetaTitle`, `썸네일 등록` …)로 필드를 찾아
`cms_mapping.json`을 만들고, 전체 폼 구조를 `cms_form_dump.json`에 남깁니다.

```
✅ category           #ca_name            select[]
✅ title              #wr_subject         input[text]
⚠️  body              #editor             textarea — 라벨 불일치, 확인 권장
❌ thumbnail          -                   찾지 못함 — 직접 입력 필요
```

`⚠️`나 `❌`가 뜨면 `cms_form_dump.json`에서 올바른 셀렉터를 찾아
`cms_mapping.json`의 `fields`에 직접 채우세요. **CMS 화면이 바뀌면 이 명령만 다시 돌리면 됩니다.**

### 2단계 — 업로드

```bash
python main.py publish 3                   # 연습 실행 (저장 안 함)
python main.py publish 3 --save            # 실제 저장, 미노출
python main.py publish 3 --save --expose   # 실제 저장, 노출 (확인 입력 필요)
python main.py publish 3 --save --show     # 브라우저 창을 띄워 보면서
python main.py publish                     # 인자 없이 실행하면 최근 글 목록
```

웹 UI(`/post/<id>`)의 **CMS 자동 업로드** 카드에서도 같은 일을 할 수 있습니다.

### 썸네일 자동 생성 — 사진 배경 + 제목

CMS가 썸네일 없이는 글을 저장하지 않으므로 생성이 파이프라인에 포함되어 있습니다.
기존 발행 썸네일과 같은 구성으로 만듭니다.

| 레이아웃 | 구성 |
|---|---|
| `center` | 제목을 화면 중앙에 크게 → 세로 구분선 → 로고 |
| `bottom` | 로고를 위에 → 세로 구분선 → 제목을 아래쪽에 크게 |
| `auto` | 글마다 번갈아 (기본값). 목록이 단조로워지지 않습니다 |

제목은 `thumbnail_copy`를, 서체는 명조/세리프 계열을 씁니다.
한글이면 나눔명조·바탕, 영문이면 Georgia·Times 계열을 자동으로 고릅니다.

#### 소재 준비

```
assets/
  ├─ logo.png              ← 배경을 지운(누끼) 투명 PNG
  └─ thumbnails/
       ├─ 공용사진.jpg
       └─ 발톱무좀치료/     ← 카테고리 폴더가 있으면 여기서 우선 선택
            └─ foot01.jpg
```

- 로고는 알파 채널의 글자 모양만 남기고 **크림색으로 다시 칠해서** 얹습니다.
  원본이 진한 글자여도 어두운 사진 위에서 읽힙니다.
  원본 색을 그대로 쓰려면 `THUMB_LOGO_TINT=0`.
- 배경 사진은 포스트URL을 해시해서 고릅니다. **같은 글은 항상 같은 사진**이 나오므로
  다시 생성해도 썸네일이 바뀌지 않습니다.
- 사진이 하나도 없으면 단색 배경으로 만들어집니다(저장은 정상 동작).

자세한 내용은 [assets/README.md](assets/README.md)에 있습니다.

#### 가독성 — 어두움막 자동 조절

사진 위에 글자를 얹으면 배경 밝기에 따라 글씨가 묻힙니다.
세기를 고정하면 밝은 사진에서는 안 읽히고 어두운 사진에서는 답답해집니다.

그래서 **글자가 놓일 영역의 실제 밝기를 재서** 필요한 만큼만 반투명 검정을 덮습니다.
제목과 로고 영역을 따로 계산하고, 경계는 smoothstep으로 이어 띠가 생기지 않게 합니다.
기준 밝기는 `THUMB_TARGET_LUMA`(기본 95)로 조절합니다. 낮출수록 어두워집니다.

#### 미리보기

웹 UI `/post/<id>`의 업로드 카드에서 썸네일을 바로 볼 수 있습니다.
**중앙형 / 하단형 / 자동**을 눌러 비교한 뒤 그대로 업로드하면 됩니다.

```bash
THUMB_PHOTO_DIR=assets/thumbnails
THUMB_LOGO=assets/logo.png
THUMB_LOGO_WIDTH=200
THUMB_LOGO_TINT=1
THUMB_LAYOUT=auto
THUMB_TEXT_COLOR=#F5EBDC
THUMB_TARGET_LUMA=95
THUMB_FONT_KO=          # 비워 두면 자동 탐지. 예: C:\Windows\Fonts\batang.ttc
THUMB_FONT_EN=
```

> **배경 사진에 시술 전후 비교 사진을 쓰지 마세요.**
> 의료법 제56조 제2항 제4·5호에 따라 시술 장면·전후 사진은 제한됩니다.
> 사용권이 확보된 사진인지도 반드시 확인하세요.

### 운영 CMS에 붙이기 전에 — 목 CMS로 연습

실제 관리자 화면과 같은 구조의 가짜 CMS가 들어 있습니다.
운영 블로그를 건드리지 않고 전 과정을 그대로 돌려 볼 수 있습니다.

```bash
# 터미널 1
python tools/mock_cms.py            # http://localhost:5002 (test / test)

# 터미널 2
export CMS_WRITE_URL=http://localhost:5002/write
export CMS_LOGIN_URL=http://localhost:5002/login
export CMS_USERNAME=test CMS_PASSWORD=test
export CMS_MAPPING_PATH=mock_mapping.json

python main.py inspect-cms          # 폼 분석이 되는지
python main.py publish 1 --save     # 업로드가 되는지
```

저장된 내용은 `mock_cms_submissions.json`에 쌓입니다.
값이 제자리에 들어갔는지 확인한 뒤 운영 설정으로 바꾸세요.

### 점검

```bash
python test_publish.py    # 썸네일 규격 · 셀렉터 매핑 · 업로드 절차 (브라우저 없이)
```

---

## 발행 후 할 일

1. CMS에서 미노출 글을 열어 내용을 확인하고 노출로 전환
2. JSON-LD를 [리치 결과 테스트](https://search.google.com/test/rich-results)로 검증
3. 구글 서치콘솔·네이버 서치어드바이저에 URL 수집 요청
4. `config.py`의 `TARGET_URLS`에 발행 URL 추가 → 다음날부터 순위 자동 추적
