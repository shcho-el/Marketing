# 윈도우에서 실행되는 스크립트는 CRLF로 고정한다.
# LF만 있으면 cmd.exe가 여러 줄 블록이나 goto에서 오작동할 수 있다.
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# 셸 스크립트는 LF를 유지해야 한다.
*.sh   text eol=lf
ob     text eol=lf

# 바이너리
*.ico  binary
*.png  binary
*.jpg  binary
*.otf  binary
*.ttf  binary
